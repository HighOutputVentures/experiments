const FPS = 30;
const slideOneDuration = 10;
const messageDuration = 5;
const lastSlideDuration = 3;

const constants = {
  FPS,
  slideOneDuration,
  messageDuration,
  lastSlideDuration,
};

export default constants;
