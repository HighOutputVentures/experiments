{
  "name": "shopee-litest",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^6.1.0"
  },
  "scripts": {
    "dev": "lerna run --parallel dev",
    "build": "lerna run --parallel build"
  }
}
